# Golden DB FastAPI service only.
# Run from this directory:
#   docker compose up --build -d
# Or from agriseva/ai:
#   docker compose -f agriseva/tools/golden/docker-compose.yml up --build -d
#
# Requires .env in this directory (see .env.example):
#   cp .env.example .env

services:
  golden-api:
    build:
      context: ../../..
      dockerfile: agriseva/tools/golden/Dockerfile
    container_name: golden-api
    ports:
      - "8110:8005"
    env_file:
      - .env
    environment:
      PYTHONUNBUFFERED: "1"
    restart: unless-stopped
